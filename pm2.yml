apps:
    - name: dorset-dream-agency
      script: "start"
      instances: 1,
      autorestart: true
      watch: false
      max_memory_restart: "500M"
      restart_delay: 1000
      env:
          NODE_ENV: production
