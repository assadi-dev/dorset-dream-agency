export type TodayDateReducerType = { day: string; hours: string; week: number };

export type getClientCountResponse = {
    count: number;
    difference: {
        count: number;
        percentage: number;
    };
};
