import { ChartConfig } from "@/components/ui/chart";

export const description = "An interactive bar chart";
export const chartConfig = {
    desktop: {
        label: "Location/Ventes",
        color: "hsl(var(--chart-1))",
    },
} satisfies ChartConfig;
