import { wait } from "@/lib/utils";
import React from "react";

const Announcement = () => {
    return (
        <div>
            Announcement <button type="button">test</button>
        </div>
    );
};

export default Announcement;
