import React from "react";
import WarrantForm from "./WarrantForm";

const AddWarrant = () => {
    const createWarrant = async (values) => {};

    return <WarrantForm onSubmitValues={createWarrant} />;
};

export default AddWarrant;
