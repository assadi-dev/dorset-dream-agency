import React from "react";
import Form from "./form";

const AddForm = () => {
    const submitForm = (values) => {};

    return (
        <div>
            <Form />
        </div>
    );
};

export default AddForm;
